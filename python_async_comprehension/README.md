# Python - Async Comprehension

This project introduces asynchronous generators and async comprehensions in Python. It shows how to produce values over time using `async def`, `await`, and `yield`, and how to collect those values with `async for` or async comprehensions.