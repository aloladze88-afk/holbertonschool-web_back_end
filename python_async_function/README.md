# Python - Async

This project introduces the basics of asynchronous programming in Python. It shows how to define coroutines with `async def`, pause them with `await`, run async code with `asyncio`, and use random delays to simulate tasks that take different amounts of time.